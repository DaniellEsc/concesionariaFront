import { FilterVehiculosPipe } from './filter-vehiculos.pipe';

describe('FilterVehiculosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVehiculosPipe();
    expect(pipe).toBeTruthy();
  });
});
